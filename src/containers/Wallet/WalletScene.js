import React, { Component } from 'react'
import { View } from 'react-native';
import { Typo } from 'coupon-components-native';

export default class WalletScene extends Component {
  render() {
    return (
      <View>
        <Typo.Title>Wallet</Typo.Title>
      </View>
    )
  }
}
