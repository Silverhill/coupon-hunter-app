# Coupon maker mobile

## Start Dev Project
 * `yarn start`

## Start Storybook
This command run storybook and expo cli with concurrently and `REACT_NATIVE_ENV=storybook` variable.
 * `yarn dev:storybook`